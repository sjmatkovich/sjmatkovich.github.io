project:
  type: website
  # output-dir defaults to _site; the site is published to the gh-pages
  # branch by CI (.github/workflows/publish.yml), not committed to main.

website:
  title: "SJM"
  navbar:
    left:
      - href: index.qmd
        text: Home
      - posts.qmd
      - about.qmd

format:
  html:
    theme: yeti
    css: styles.css
    toc: true
    page-layout: full

editor: visual

